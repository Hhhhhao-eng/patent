spring.application.name=kgpc

# 应用配置
server.port=8080
spring.application.name=django-integration-service

# Django 服务配置
django.ip=127.0.0.1
django.port=8000

# 日志配置
logging.level.org.springframework=INFO
logging.level.com.example=DEBUG